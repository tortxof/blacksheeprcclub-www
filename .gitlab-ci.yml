image: tortxof/webdev

variables:
  JEKYLL_ENV: production

before_script:
  - bower install

stages:
  - build

jekyll:
  stage: build
  script:
    - jekyll build
  tags:
    - webdev
  only:
    - master
